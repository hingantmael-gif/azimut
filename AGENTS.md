# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

## Features WIP

Toute zone en travaux : visible + casque de chantier, **non cliquable**.
Composants : `src/ui/ComingSoon.tsx` (`ComingSoonLock` / `ComingSoonOverlay`).
Règle : `.cursor/rules/wip-coming-soon.mdc`.
