import { View } from 'react-native';

/**
 * Placeholder — l’onglet « Nouveau » redirige vers /program/new
 * via le listener tabPress du layout.
 */
export default function CreateTabPlaceholder() {
  return <View style={{ flex: 1 }} />;
}
